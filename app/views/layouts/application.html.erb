<!DOCTYPE html>
<html>
<head>
  <title>Test</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>

<body style="background:lightgray">

  <div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to 'Blog', root_path, :class => 'brand' %>
        <div class="nav-collapse collapse">
            <ul class="nav">
              <%= content_tag 'li', link_to('Home', root_path), :class => "#{'active' if request.path == root_path}" %>
              <%= content_tag 'li', link_to('New Blog Entry', new_blog_entry_path), :class => "#{'active' if request.path == new_blog_entry_path}" %>
              <%= content_tag 'li', link_to('Create new user' , new_user_path), :class => "#{'active' if request.path == new_user_path}" %>
              <% if current_user %>
              <%= content_tag 'li', link_to('Logout' , new_session_path), :class => "#{'active' if request.path == new_user_path}" %>
              <% else %>
              <%= content_tag 'li', link_to('Login' , new_session_path), :class => "#{'active' if request.path == new_user_path}" %>
              <% end %>
            </ul>
        </div>
      </div>
    </div>
  </div>
    
  <div class="container" id="content">
    <%= yield %>
  </div> 
</body>
</html>

